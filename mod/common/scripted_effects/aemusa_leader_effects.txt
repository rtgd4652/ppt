# 创建爱缪莎。她初始为行政官，并在创建时保存为全局事件目标。
aemusa_summon_leader = {
    create_leader = {
        class = official # 初始职业：行政官。
        name = "aemusa" # 使用本地化键显示姓名“爱缪莎”。
        gender = female # 领袖性别。
        species = owner_main_species # 使用帝国主物种，避免创建领袖时出现 species: none 错误。
        custom_description = aemusa_leader_background # 使用中央庭七人众的专属来历，替代默认 job_ 文本。
        background_ethic = ethic_spiritualist # 以唯心背景强调其与神器、命运和中央庭的联系。
        event_leader = yes # 标记为事件领袖，便于作为独特角色管理。
        skill = 11 # 初始等级为 11 级；满级由全局定义调整为 30 级。
        randomize_traits = no # 禁止随机特质，确保只拥有专属技能。
        skip_background_generation = yes # 不生成随机背景故事。
        traits = {
            trait = leader_trait_aemusa_official # 初始专属技能：命运织网。
        }
        effect = {
            aemusa_refresh_portrait = yes # 根据等级选择城市通讯立绘或 30 级专属立绘。
            set_leader_flag = aemusa_unique_leader # 供后续逻辑识别此独特领袖。
            save_global_event_target_as = aemusa_leader # 供通讯法令和事件持续定位该领袖。
        }
    }
}

# 将已保存的爱缪莎切换为指挥官，并替换为指挥官专属技能。
aemusa_become_commander = {
    event_target:aemusa_leader = {
        remove_trait = leader_trait_aemusa_commander # 先清除旧的指挥官技能（重复选择时安全）。
        remove_trait = leader_trait_aemusa_official # 清除行政官专属技能。
        remove_trait = leader_trait_aemusa_scientist # 清除科学家专属技能。
        change_leader_class = commander # 正式变更职业为指挥官。
        add_trait = {
            trait = leader_trait_aemusa_commander # 添加“逆命之牌”。
            show_message = no # 不显示原版特质获得弹窗。
            consume_selection = yes # 计入本次特质选择，避免额外可选槽。
        }
        aemusa_refresh_portrait = yes # 职业变化后按当前等级恢复正确肖像。
    }
}

# 将已保存的爱缪莎切换为行政官，并替换为行政官专属技能。
aemusa_become_official = {
    event_target:aemusa_leader = {
        remove_trait = leader_trait_aemusa_commander # 清除指挥官专属技能。
        remove_trait = leader_trait_aemusa_official # 清除旧的行政官技能（重复选择时安全）。
        remove_trait = leader_trait_aemusa_scientist # 清除科学家专属技能。
        change_leader_class = official # 正式变更职业为行政官。
        add_trait = {
            trait = leader_trait_aemusa_official # 添加“命运织网”。
            show_message = no # 不显示原版特质获得弹窗。
            consume_selection = yes # 计入本次特质选择。
        }
        aemusa_refresh_portrait = yes # 职业变化后按当前等级恢复正确肖像。
    }
}

# 将已保存的爱缪莎切换为科学家，并替换为科学家专属技能。
aemusa_become_scientist = {
    event_target:aemusa_leader = {
        remove_trait = leader_trait_aemusa_commander # 清除指挥官专属技能。
        remove_trait = leader_trait_aemusa_official # 清除行政官专属技能。
        remove_trait = leader_trait_aemusa_scientist # 清除旧的科学家技能（重复选择时安全）。
        change_leader_class = scientist # 正式变更职业为科学家。
        add_trait = {
            trait = leader_trait_aemusa_scientist # 添加“星图演算”。
            show_message = no # 不显示原版特质获得弹窗。
            consume_selection = yes # 计入本次特质选择。
        }
        aemusa_refresh_portrait = yes # 职业变化后按当前等级恢复正确肖像。
    }
}

# 按爱缪莎当前等级刷新肖像：30 级使用成长后的专属立绘，其余等级使用城市通讯立绘。
aemusa_refresh_portrait = {
    if = {
        limit = {
            has_leader_flag = aemusa_level_30_portrait # 达到 30 级后由升级事件设置。
        }
        change_leader_portrait = aemusa_portrait_level_30 # 30 级专属肖像。
    }
    else = {
        change_leader_portrait = aemusa_portrait # 常规城市背景肖像。
    }
}

# 创建赛斯。赛斯作为白夜馆神官接入，初始为行政官并保存为全局事件目标。
seth_summon_leader = {
    create_leader = {
        class = official # 初始职业：行政官。
        name = "seth" # 使用本地化键显示姓名“赛斯”。
        gender = male # 领袖性别。
        species = owner_main_species # 使用帝国主物种，确保事件领袖属于当前神器使文明。
        custom_description = seth_leader_background # 使用白夜馆神官的专属背景文本。
        background_ethic = ethic_spiritualist # 以唯心背景强调神官、信仰与守护者身份。
        event_leader = yes # 标记为事件领袖，便于作为独特角色管理。
        skill = 7 # 初始等级为 7 级，低于爱缪莎但高于普通招募领袖。
        randomize_traits = no # 禁止随机特质，确保只拥有专属技能。
        skip_background_generation = yes # 不生成随机背景故事。
        traits = {
            trait = leader_trait_seth_official # 初始专属技能：羽蛇神杖。
        }
        effect = {
            set_leader_flag = seth_unique_leader # 供后续逻辑识别此独特领袖。
            save_global_event_target_as = seth_leader # 供白夜馆名册和后续互动持续定位赛斯。
        }
    }
}

# 创建幽桐。幽桐作为主线剧情与记忆殿堂档案接入，初始为指挥官并保存为全局事件目标。
yutong_summon_leader = {
    create_leader = {
        class = commander # 初始职业：指挥官，对应原作射手定位。
        name = "yutong" # 使用本地化键显示姓名“幽桐”。
        gender = male # 领袖性别。
        species = owner_main_species # 使用帝国主物种，确保事件领袖属于当前神器使文明。
        custom_description = yutong_leader_background # 使用幽桐的专属背景文本。
        background_ethic = ethic_militarist # 以尚武背景强调射手、战斗与远程压制定位。
        event_leader = yes # 标记为事件领袖，便于作为独特角色管理。
        skill = 7 # 初始等级为 7 级，与赛斯保持同一接入档位。
        randomize_traits = no # 禁止随机特质，确保只拥有专属技能。
        skip_background_generation = yes # 不生成随机背景故事。
        traits = {
            trait = leader_trait_yutong_commander # 初始专属技能：神弓・甘狄拔。
        }
        effect = {
            set_leader_flag = yutong_unique_leader # 供后续逻辑识别此独特领袖。
            save_global_event_target_as = yutong_leader # 供白夜馆名册和后续互动持续定位幽桐。
        }
    }
}

# 创建拉比。拉比作为主线剧情与记忆殿堂档案接入，初始为指挥官并保存为全局事件目标。
rabi_summon_leader = {
    create_leader = {
        class = commander # 初始职业：指挥官，对应原作战士定位。
        name = "rabi" # 使用本地化键显示姓名“拉比”。
        gender = male # 领袖性别。
        species = owner_main_species # 使用帝国主物种，确保事件领袖属于当前神器使文明。
        custom_description = rabi_leader_background # 使用拉比的专属背景文本。
        background_ethic = ethic_militarist # 以尚武背景强调战士、刚属性与地面突破定位。
        event_leader = yes # 标记为事件领袖，便于作为独特角色管理。
        skill = 7 # 初始等级为 7 级，与赛斯、幽桐保持同一接入档位。
        randomize_traits = no # 禁止随机特质，确保只拥有专属技能。
        skip_background_generation = yes # 不生成随机背景故事。
        traits = {
            trait = leader_trait_rabi_commander # 初始专属技能：怪兽・阿米特。
        }
        effect = {
            set_leader_flag = rabi_unique_leader # 供后续逻辑识别此独特领袖。
            save_global_event_target_as = rabi_leader # 供白夜馆名册和后续互动持续定位拉比。
        }
    }
}
