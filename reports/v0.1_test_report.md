# 《神器使》Stellaris Mod v0.1.0 测试报告

> 候选构建日期：2026-07-11
> 测试分支：`parallel/v1-integration`
> 当前结论：静态检查通过，游戏内复测待执行
> 发布门槛：十二项手动测试与专项测试完成后，才能合并和创建 `v0.1.0` 标签。

## 静态检查结果

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 非本地化脚本 BOM | 通过 | `.txt`、`.gfx`、`.asset`、`.gui`、`.mod` 未发现 BOM |
| 简体中文本地化 BOM | 通过 | 主本地化文件保留 UTF-8 BOM |
| 花括号数量 | 通过 | 基础扫描未发现不匹配文件 |
| 无效 `trade_value` | 通过 | 已替换为 4.4.3 的 `trade` |
| 显式 `texturefile` 路径 | 通过 | 已注册的显式纹理路径均存在 |
| 命运之主名称表 | 已修复待复测 | 使用 `use_shipnames_from = titan` |
| 正式图标完整性 | 已知限制 | 41 个隐式图标待统一美术规范后制作 |

最新可见 `error.log` 生成于 2026-07-08 20:56，早于本轮修复，不能作为最终验收日志。

## 1. 白夜馆入口稳定

### 测试目标

确认白夜馆入口可以反复打开、返回和关闭，并在读档后继续使用。

### 测试步骤

新建神器使帝国，打开白夜馆，依次进入通讯、名册和回忆页面；重复进入并保存读档后复测。

### 预期结果

所有页面稳定切换，不崩溃，不出现重复入口或无法返回。

### 当前状态

上一轮通过；v0.1.0 候选构建待复测。

### 问题记录

暂无新的候选构建记录。

### 修复建议

若失败，优先记录触发页面、事件 ID、存档阶段与最新日志行。

## 2. 招募角色稳定

### 测试目标

确认爱缪莎、赛斯、幽桐和拉比都能通过白夜馆招募。

### 测试步骤

在同一新开局中依次招募四名角色，检查领袖列表和对应通讯页面。

### 预期结果

四名领袖均被创建一次，姓名、肖像和职业可见。

### 当前状态

上一轮通过；v0.1.0 候选构建待复测。

### 问题记录

角色与物种肖像裁切需要同时目视确认。

### 修复建议

若肖像异常，记录具体界面和截图，不直接修改原始立绘。

## 3. 已招募角色不重复出现

### 测试目标

确认招募 flag 和唯一角色判断在重复打开界面及读档后仍有效。

### 测试步骤

招募角色后重新进入名册，重复点击入口，保存并重新载入。

### 预期结果

不再显示可重复招募选项，不创建第二名同名领袖。

### 当前状态

上一轮通过；v0.1.0 候选构建待复测。

### 问题记录

暂无新的候选构建记录。

### 修复建议

检查国家 flag、领袖标记和事件选项触发条件是否同时存在。

## 4. 领袖职业正确

### 测试目标

确认四名角色职业符合 v0.1 设计，爱缪莎职业切换不会产生无效领袖。

### 测试步骤

检查四名领袖详情；分别执行爱缪莎可用的职业切换并重新打开领袖界面。

### 预期结果

赛斯为行政官，幽桐和拉比为指挥官；爱缪莎按选择成为正确职业。

### 当前状态

上一轮通过；v0.1.0 候选构建待复测。

### 问题记录

暂无新的候选构建记录。

### 修复建议

若失败，核对创建效果中的 `class`、职业 trait 和旧领袖替换逻辑。

## 5. trait 正确挂载

### 测试目标

确认每名神器使获得对应专属 trait，且不存在缺失、重复或挂错职业。

### 测试步骤

招募四名角色，检查领袖详情和任职后的实际修正。

### 预期结果

专属 trait 正确挂载，职业切换后的爱缪莎只保留目标职业 trait。

### 当前状态

上一轮通过；v0.1.0 候选构建待复测。

### 问题记录

正式 trait 图标尚未制作，但不影响 trait 逻辑验收。

### 修复建议

逻辑错误与图标缺失分开记录；本轮只修逻辑错误。

## 6. 等级上限 30 生效

### 测试目标

确认神器使可以达到 30 级，普通领袖不会误用神器使等级逻辑。

### 测试步骤

使用测试手段提升一名神器使至 30 级，同时观察普通领袖上限和事件触发。

### 预期结果

神器使可达到 30 级，普通领袖不受影响；爱缪莎等级立绘逻辑只作用于本人。

### 当前状态

上一轮通过；v0.1.0 候选构建待复测。

### 问题记录

30 级立绘切换仍需目视确认。

### 修复建议

检查角色 flag、等级触发条件和肖像切换事件作用域。

## 7. 专属飞升可正常选择

### 测试目标

确认神器使专属飞升显示、前置、选择和本地化正常。

### 测试步骤

使用符合条件的神器使帝国测试正向选择，并用非神器使帝国进行反向验证。

### 预期结果

符合条件时可选，不符合条件时不可选，不出现缺失 key。

### 当前状态

上一轮通过；v0.1.0 候选构建待复测。

### 问题记录

暂无新的候选构建记录。

### 修复建议

核对 `potential`、`possible`、前置科技与物种判断。

## 8. 专属传统可正常打开

### 测试目标

确认专属传统类别、传统树、前置关系和本地化可用。

### 测试步骤

使用神器使帝国打开传统界面并依次选择节点；使用普通帝国反向检查。

### 预期结果

神器使帝国可正常采用，普通帝国不会错误获得。

### 当前状态

上一轮通过；v0.1.0 候选构建待复测。

### 问题记录

暂无新的候选构建记录。

### 修复建议

检查传统类别触发、节点前置与 `_delayed` 本地化键。

## 9. 科技线可正常抽取与解锁

### 测试目标

确认专属科技能够进入抽取池并解锁正确内容，同时验证专属星球不会重复生成。

### 测试步骤

正常研究关键科技；刷新科技、等待月结、保存读档并重复触发相关事件。

### 预期结果

科技可抽取且不死锁；专属星球第一次生成后不再创建第二颗。

### 当前状态

曾修复后通过；v0.1.0 候选构建待专项复测。

### 问题记录

曾出现“专属星球会一直刷新”，已通过国家 flag 防重复修复。

### 修复建议

若再次出现，检查所有科技、事件、on_action、法令和月度逻辑是否统一检查并设置同一个永久 flag。

## 10. 专属星球区划可正常建造

### 测试目标

确认专属星球上的区划、建筑、岗位、资源与限制正常工作。

### 测试步骤

建造各类专属区划和代表性建筑，等待岗位生成与月结。

### 预期结果

区划和建筑可以建造，岗位正常出现，资源结算不产生无效资源错误。

### 当前状态

静态资源键已修复；游戏内待复测。

### 问题记录

本轮已将建筑和岗位中的 `trade_value` 分别改为 `trade`。

### 修复建议

若月结报错，记录具体建筑、岗位与资源块，不进行额外平衡调整。

## 11. 本地化完整

### 测试目标

确认 v0.1 核心对象在简体中文环境中具有名称与描述。

### 测试步骤

检查物种、起源、白夜馆、领袖、trait、飞升、传统、科技、区划、建筑、舰船和巨构界面。

### 预期结果

不显示内部 key，不出现空白名称或阻断理解的英文占位。

### 当前状态

上一轮通过；命运之主名称兼容修复后待复测。

### 问题记录

缺失正式图标不计为本地化失败。

### 修复建议

记录完整内部 key，并在主简体中文本地化文件中补充对应文本。

## 12. error.log 无关键红字

### 测试目标

确认当前候选构建没有本 Mod 引起的脚本解析、无效对象、事件崩溃或缺失必需组件错误。

### 测试步骤

备份旧日志，只启用本 Mod 启动新开局，完成本报告全部操作后退出并检查新日志。

### 预期结果

不存在引用本项目文件或 key 的功能性硬错误。

### 当前状态

待生成新日志。

### 问题记录

2026-07-08 旧日志中的 GFX BOM、`trade_value` 和命运之主名称表错误已在代码中修复；岗位、修正、trait 和巨构图标警告按用户决定延后处理。

### 修复建议

只忽略能明确归属其他 Mod 的错误，以及本报告登记的缺失正式图标警告；其他项目相关错误继续阻断发布。

## 命运之主与命运观测塔专项

- [ ] 命运之主进入设计器或规定建造路径。
- [ ] 反应堆、传感器、跃迁核心、推进器、作战电脑和两个光环均存在。
- [ ] 舰船可以正常超空间航行并使用跃迁功能。
- [ ] 不再出现名称表无法生成 `destiny_lord` 舰级名的错误。
- [ ] 命运之主被摧毁后按当前设计重构。
- [ ] 命运观测塔四阶段均可建造。
- [ ] 命运观测塔政策模式可切换并生效。

## 最终结论

当前只能确认“静态封版修复完成”。在手动复测和新日志检查完成前，不得将本报告改为“发布通过”，也不得创建 `v0.1.0` 正式标签。
